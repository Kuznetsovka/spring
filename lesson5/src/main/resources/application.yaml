server:
  port: 8090
  servlet:
    context-path: /app

spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:mysql://localhost:3306/market?serverTimezone=UTC
    username: root
    password: Nbirf


